---
import type { HTMLAttributes } from 'astro/types';
import { cn } from '@/lib/utils';

type Props = HTMLAttributes<'span'>;

const { class: className, ...props } = Astro.props;
---

<span class={cn('flex items-center gap-1', className)} {...props}>
  <span class="animate-plop size-1 rounded-full bg-current"></span>
  <span
    class="animate-plop size-1 rounded-full bg-current"
    style="animation-delay: 200ms;"></span>
  <span
    class="animate-plop size-1 rounded-full bg-current"
    style="animation-delay: 400ms;"></span>
</span>
